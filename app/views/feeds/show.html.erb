<h1><%= @feed.name %></h1>
<% @articles.each do | article | %>
    <h2><%= link_to article.title, article.link, target: "_blank" %></h2>
    <p>
    <%= article.description %>
    </p>
    <span>
        <%= article.date.to_s.split(" ").slice(0,4).join(" ") %>
    </span>
<% end %>